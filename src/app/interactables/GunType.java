package app.interactables;

/**
 * GunType contains an enum of all available guns that can be used when creating maps
 *
 * @author Oliver Björklund
 * @version 1.0
 */
public enum GunType {
    GUN,
    RIFLE,
    CROSSBOW,
    BOMB
}
